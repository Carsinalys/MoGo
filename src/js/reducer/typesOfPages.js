export default (state, action) => {
  let currentTypePage = 'main';
  // const {type} = action;
  // switch (type) {
  //   case 'main': return currentPage = 1;
  //   case 'about': return currentPage = 2
  // }

  return currentTypePage
}